import { AccountPageSkeleton } from "@/components/system/page-skeletons";

/**
 * Loading state for the account area — greeting, stat grid, activity panels.
 */
export default function Loading() {
    return <AccountPageSkeleton />;
}